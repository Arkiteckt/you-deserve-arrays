const prompt =  require('prompt-sync')({ sigint: true});

let userArray = (prompt("Can you give me an Array Barray?: "));

var randomStuff = ["go cart", "helicopter", "motorcycle"]

var x = 0
console.log(x > [randomStuff.length - 1]);
//{
    //console.log (randomStuff[x]);
   // x--
//}


//Write a program, getLastItemFrom.js that prompts the user for an array, then logs
// out the last item from that array


//const lastItem = colors[colors.length -1]